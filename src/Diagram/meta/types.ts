export enum NodeType {
	Input = "input",
	Output = "output",
}

export enum NodeColor {
	Input = "#6ede87",
	Output = "#6865A5",
	Default = "#ff0072",
}

export enum EdgeType {
	Bezier = "default",
	Smoothstep = "smoothstep",
	Step = "step",
	Straight = "straight",
}
